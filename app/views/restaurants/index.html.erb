<div class="container mt-3">
  <h1>Restaurants</h1>

  <ul>
    <% @restaurants.each do |restaurant| %>
      <li><%= link_to restaurant.name, restaurant_path(restaurant) %> | <%= link_to "🔪", restaurant_path(restaurant), method: :delete, data: {confirm: "Are you sure you want to delete this?"}, remote: true %></li>
    <% end %>
  </ul>
</div>